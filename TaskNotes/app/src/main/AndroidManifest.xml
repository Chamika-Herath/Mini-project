<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tasknotes">

    <!--
        This app does NOT require any permissions.
        SECURE CODING PRACTICE #8 - Principle of Least Privilege:
        We request no unnecessary permissions (no INTERNET, no STORAGE, etc.)
        since the app operates entirely offline using Room's internal database.
        Requesting only needed permissions minimizes the app's attack surface.
    -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TaskNotes">

        <!-- Main Activity - entry point showing the task list -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Add/Edit Task Activity -->
        <activity
            android:name=".ui.AddEditTaskActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" />

    </application>

</manifest>
